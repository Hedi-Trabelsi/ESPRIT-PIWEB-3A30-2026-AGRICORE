# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Use the same database as dev (no _test suffix). The CRUD tests clean up after themselves.
DATABASE_URL="mysql://root:@127.0.0.1:3306/projetjava"
